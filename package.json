{
    "name": "sims",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "start": "npm run clean; parcel index.html",
        "deploy": "npm run clean; parcel build index.html --public-url /sims/; python3 copy-glsl-files.py",
        "clean": "rm -rf ./dist; rm -rf ./.parcel-cache;",
        "format": "prettier --write ./"
    },
    "dependencies": {
        "p5": "*"
    },
    "devDependencies": {
        "@parcel/transformer-glsl": "^2.10.3",
        "@types/p5": "*",
        "parcel": "^2.10.3",
        "prettier": "*",
        "typescript": "*"
    }
}
